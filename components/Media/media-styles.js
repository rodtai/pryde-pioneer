import { StyleSheet } from 'react-native';

export default styles = StyleSheet.create({  
    video: {
        position: 'absolute',
        top: 0,
        left: 0,
        bottom: 0,
        right: 0,
    },
});